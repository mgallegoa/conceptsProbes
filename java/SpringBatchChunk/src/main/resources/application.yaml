server:
  port: 3000
spring:
  application:
    name: SpringBatchChunk
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  batch:
    job:
      enable: false
    jdbc:
      initialize-schema: always
  sql:
    init:
      mode: always
